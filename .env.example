# Copy this file to `.env` and fill in your key.
# Only the Search Assistant needs this (OpenRouter, OpenAI-compatible API).
# Get a key at https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_api_key_here
